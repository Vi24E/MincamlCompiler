- if-fold
- loop argの固定化
- if ... then () else ...やif ... then ... else ()の簡略化
- 命令リオーダリング
- Global変数のlivein/liveout
-- Globalが生存している時には1命令あたりコスト1
-- Globalを読み出し/保存するのには1命令あたりコストS(~ 50)
-- 以上に従ってコストを最小化する。これはそれぞれの変数についてdp[index][state] := min_costで、計算できる。(ただし変数がuseされているにも拘らず、state=liveoutの時はコストinfとする。)

- use and for if


関数がある時、引数の処理がbeta等で正しくない？関数が残るiter = 1でバグ

- 	add	%i31, %i0, %i31
- movi	%i31, 4
  ceq	%i31, %i6, %i31
  jzero	%i0, %i31, Lrelax_skip_54
- ceq	%i31, %i5, %i0
  jzero	%i0, %i31, Lrelax_skip_55


sw	%i7, 292(%i0)
sw	%i0, 292(%i0)

lw	%i6, 138(%i1)
sw	%i31, 138(%i1)

sf	%f31, 129(%i1)
lf	%f31, 129(%i1)

fleq %i4, %f5, %f31     ; 比較結果 → 整数0/1
itof %f1, %i4            ; ← 0/1をfloatに変換
ftern %f5, %f1, %f5, %f3 ; 条件選択

2 の「ランダムで Accuracy が落ちる原因」は、かなり高確率でこれです。

victim 選定の禁止条件が「その命令で使っているか」だけで、live-out を見ていない
spilling.rs (line 700)
そのため、need_load だけのケースで victim を上書きしても復元されず、後続で必要な値を壊しうる
通常命令側: spilling.rs (line 342)
分岐命令側: spilling.rs (line 410)
use最小 だとたまたま壊れにくいレジスタを選び続けて症状が隠れ、ランダムだと壊れやすいレジスタを引いて Accuracy 低下が顕在化します。

